import { DocsClient } from "./docs-client";

export const metadata = {
 title: "Документация CRM | MerchCRM",
 description: "Архитектура, схемы взаимодействия, описание модулей CRM-системы",
};

export default function DocsPage() {
 return <DocsClient />;
}
