"use client";

import { ThreeViewer } from "./three-viewer/ThreeViewer";

/**
 * ThreeViewer wrapper that maintains backward compatibility
 * but uses a refactored, split architecture internally.
 */
export default ThreeViewer;
