# 🖼️ Система управления аватарами

## Быстрый старт

### Автоматическое удаление старых аватаров
При загрузке нового аватара система **автоматически удаляет** предыдущий файл:

```typescript
// Просто загрузите новый файл - старый удалится сам
const avatarPath = await saveAvatarFile(buffer, userId, oldAvatarPath);
```

### Очистка неиспользуемых файлов
Запустите скрипт для удаления "осиротевших" аватаров:

```bash
npx tsx scripts/cleanup-avatars.ts
```

## Хранилище

- **Локация**: `/public/uploads/avatars/`
- **Формат**: `{userId}-{timestamp}.jpg`
- **Тип**: Локальное хранилище сервера (не S3)

## Документация

Полная документация: [docs/AVATAR_STORAGE.md](./docs/AVATAR_STORAGE.md)

---

**Важно**: Аватары хранятся локально для быстрого доступа. S3 используется только для документов заказов и временных файлов.
