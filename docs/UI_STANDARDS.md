# Стандарты пользовательского интерфейса (UI Standards)

## 📦 Складской учет (Warehouse)

### Фильтрация позиций (Items Filtering)
Для списков товаров и материалов на складе **всегда** использовать следующий стандартный набор фильтров по статусу остатков:

1.  **Все** (All) — отображение всех позиций без исключения.
2.  **В наличии** (In stock) — товары, количество которых превышает минимальный порог (`quantity > lowStockThreshold`).
3.  **Заканчиваются** (Running low) — товары, количество которых больше 0, но меньше или равно минимальному порогу (`0 < quantity <= lowStockThreshold`).
4.  **Нет в наличии** (Out of stock) — товары с нулевым остатком (`quantity === 0`).

**Важно:** Не использовать сокращения вроде "Мало" или "Нет". Использовать полные названия для предотвращения двусмысленности.

### Визуальное отображение
*   Фильтры должны быть реализованы в виде переключаемых кнопок (tabs/pills) или сегментированного контроля.
*   Кнопка "Все" является состоянием по умолчанию.

### Навигационная цепочка (Breadcrumbs)
*   **Расположение**: Хлебные крошки должны располагаться **над** основным заголовком страницы и управляющими элементами.
*   **Отступ**: Минимальный вертикальный отступ от заголовка (например, `space-y-2` в родительском контейнере).
*   **Стиль**:
    *   Шрифт: мелкий (`text-xs` или `text-[10px]`), жирный (`font-bold`), верхний регистр (`uppercase`), разрядка (`tracking-widest`).
    *   Цвет: `text-slate-400` для неактивных/промежуточных элементов, `text-slate-500` для текущего (последнего) элемента.
    *   Hover-эффект: `hover:text-indigo-600` для кликабельных ссылок.
    *   Разделитель: `/` цвета `text-slate-300`.
*   **Компонент**: Использовать стандартный компонент `components/ui/breadcrumbs.tsx`.
### Проектирование многошаговых форм (Multi-step Form Layout)
Для сложных форм создания или редактирования сущностей всегда использовать паттерн "Студийный интерфейс":

1.  **Фиксированная высота**: Общий контейнер страницы должен иметь фиксированную высоту (например, `h-[calc(100vh-130px)]`) и `overflow-hidden`, чтобы предотвратить прокрутку всей страницы браузера.
2.  **Сайдбар навигации**:
    *   Ширина: фиксированная (например, `320px`).
    *   Структура: `flex flex-col`. Верхняя часть — заголовок и навигационные ссылки, средняя (`flex-1`) — список шагов с `overflow-y-auto`, нижняя (`shrink-0`) — блок сохранения черновика или управления сессией.
    *   Стиль активного шага: черный фон (`bg-slate-900`), белые шрифты, тень.
3.  **Основной блок контента**:
    *   Обертка: `flex-1 flex flex-col min-h-0`.
    *   Карточка шага: Белый фон (`bg-white`), скругление (`rounded-[24px]`), тень.
    *   Внутренняя структура:
        *   Область контента: `flex-1 overflow-y-auto pb-12`. Обязательно `min-h-0` для корректной работы flex-scrolling.
        *   Нижняя панель (Футер): `shrink-0 h-[72px]` для кнопок "Назад" и "Далее". Всегда зафиксирована за пределами области прокрутки.
4.  **Адаптивность и растяжение**:
    *   Если контента мало, область `flex-1` должна растягиваться, удерживая футер в самом низу карточки.
    *   Если контента много, появляется вертикальная прокрутка только внутри контентной области, заголовок и футер шага остаются на месте.
5.  **Единообразие**: Все кнопки навигации в формах должны использовать общий компонент `StepFooter` для обеспечения идентичного внешнего вида и поведения.

## 📱 Мобильные шторки (BottomSheet / ResponsiveModal)

### Закреплённый футер (Sticky Footer)
Во всех модальных окнах и шторках, использующих `ResponsiveModal` или `BottomSheet`, блок кнопок действий (футер) **ДОЛЖЕН** быть закреплён внизу и **НЕ ДОЛЖЕН** скроллиться вместе с контентом.

#### Правила реализации:
1.  **Структура компонента**: Контент диалога должен быть обёрнут в `<div className="flex flex-col h-full overflow-hidden">`.
2.  **Скроллящаяся область**: Основной контент формы должен иметь класс `overflow-y-auto flex-1`.
3.  **Футер**: Блок кнопок должен быть вынесен за пределы скроллящейся области или использовать `sticky bottom-0 shrink-0`.

#### Стандартная структура диалога:
```tsx
<ResponsiveModal isOpen={isOpen} onClose={onClose}>
    <div className="flex flex-col h-full overflow-hidden">
        {/* Header */}
        <div className="p-6 pb-2 shrink-0">
            <h2>Заголовок</h2>
        </div>
        
        {/* Scrollable Content */}
        <div className="flex-1 overflow-y-auto px-6">
            <form>...</form>
        </div>
        
        {/* Fixed Footer - НЕ скроллится */}
        <div className="sticky bottom-0 p-5 pt-3 flex gap-3 shrink-0 bg-white border-t border-slate-100">
            <button>Отмена</button>
            <button>Сохранить</button>
        </div>
    </div>
</ResponsiveModal>
```

#### Стиль кнопок футера:
*   **Выравнивание**: `flex-1 w-0` для равной ширины кнопок (50/50).
*   **Высота**: `h-11`.
*   **Горизонтальные отступы**: `px-4` (мобильные), `lg:px-8` или `lg:px-10` (десктоп).
*   **Адаптивность**: На десктопе `lg:flex-none lg:w-auto` для автоматической ширины по содержимому.
