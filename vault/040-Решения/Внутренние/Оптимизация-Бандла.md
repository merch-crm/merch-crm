---
название: "Оптимизация-Бандла"
дата: 2026-03-31
обновлено: 2026-03-31
статус: активный
теги:
  - решение
  - performance
---

# Оптимизация бандла

## Проблема
Проект использует множество тяжёлых библиотек иконок и UI-компонентов (lucide-react, phosphor-icons, tabler-icons, recharts, framer-motion). Без оптимизации они попадают в бандл целиком.

## Решение
Используется `optimizePackageImports` в `next.config.ts`:

```typescript
experimental: {
    optimizePackageImports: [
        "lucide-react",
        "@phosphor-icons/react",
        "@tabler/icons-react",
        "react-icons",
        "date-fns",
        "recharts",
        "framer-motion",
        "@radix-ui/react-icons",
    ],
}
```

### Как это работает
Next.js перехватывает бочковые импорты (barrel imports) вида:
```typescript
import { User, Settings, Home } from "lucide-react";
```
И автоматически преобразует их в точечные (tree-shakeable):
```typescript
import User from "lucide-react/dist/esm/icons/user";
import Settings from "lucide-react/dist/esm/icons/settings";
```

### Дополнительные оптимизации
- `@next/bundle-analyzer` включается через `ANALYZE=true npm run build`
- `removeConsole: true` в production — удаляет все `console.log`
- `output: "standalone"` — минимальная Docker-сборка
- Оптимизация изображений: WebP формат, агрессивный кеш (31536000 сек в production)

## Файлы
- `next.config.ts` — вся конфигурация оптимизации

---
[[020-Архитектура/Технический-паспорт|← Технический паспорт]]
